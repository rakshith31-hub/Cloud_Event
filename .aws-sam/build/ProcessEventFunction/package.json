{
  "name": "cloud-event",
  "version": "1.0.0",
  "description": "Distributed, fault-tolerant event processing pipeline: S3 -> Lambda -> DynamoDB, with SQS dead-letter queue for failed events.",
  "main": "src/processEvent.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.606.0",
    "@aws-sdk/client-s3": "^3.606.0",
    "@aws-sdk/lib-dynamodb": "^3.606.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
